import ToBuyList from "../components/to-buy-list";

export default async function ToBuyListPage() {

  // const supabase = createServerComponentClient<Database>({
  //   cookies,
  // })

  // const { data: {session},} = await supabase.auth.getSession()

  return (
    <>
    {/* <SnackbarProvider> */}
    {/* <main>
    <ToBuyList />
    </main> */}
    {/* </SnackbarProvider> */}
    </>
  );
}
